/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Home_band from './Home_band';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Home_band);
